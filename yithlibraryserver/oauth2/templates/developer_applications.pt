<!DOCTYPE html>
<html lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      i18n:domain="yithlibraryserver"
      metal:use-macro="profile.macros['profile']">

<tal:block metal:fill-slot="header-title" i18n:translate="">Developer applications</tal:block>

<tal:block metal:fill-slot="content">

  <div class="page-header">
    <h1 i18n:translate="">Developer Applications</h1>
  </div>

  <p class="well well-lg" tal:condition="len(applications) == 0" i18n:translate="">You have not registered any developer application yet.</p>

  <table class="table table-striped" tal:condition="len(applications) > 0">
    <thead>
      <th i18n:translate="">Name</th>
      <th i18n:translate="">Main URL</th>
      <th i18n:translate="">Callback URL</th>
    </thead>
    <tbody>
      <tr tal:repeat="app applications">
        <td><a href="${request.route_path('oauth2_developer_application_edit', app=app.id)}">${app.name}</a></td>
        <td>${app.main_url}</td>
        <td>${app.callback_url}</td>
      </tr>
    </tbody>
  </table>

  <p><a class="btn btn-primary" href="${request.route_path('oauth2_developer_application_new')}" i18n:translate="">Register new application</a></p>

</tal:block>

</html>
